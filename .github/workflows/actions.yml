name: CI/CD Needs
on: [push]
jobs:
  checkout:
    steps:
      - uses: actions/checkout@v2
  setup-node:
    steps:
      - uses: actions/setup-node@v2
        with:
          node-version: '17'
      - run: yarn
  lint:
    steps:
      - run: yarn lint
  build-js:
    steps:
      - run: yarn build:prod
